# Task 02 Answer

## 1. Q:*为什么 `./tools/recruit-info` 能运行，而直接输入 `recruit-info` 可能找不到?*
   
      A:前者提供了明确的路径,在当前文件夹里找到tools,然后就能找到recruit-info.
        后者Shell找不到,是因为tools不在PATH里,所以找不到.


## 2. Q:*Shell 输入 `ls` 时如何找到程序？*

      A:Shell去PATH环境变量里面的目录挨个查找


     
