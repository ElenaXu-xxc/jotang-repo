# Task 08 Answer

请在这里简短回答：为什么 Shell 中 `$var` 和 `"$var"` 有时会产生不同的结果？
-----------------------------------------------------------------------
在Shell中, `$var` 会在变量展开后执行**单词分割**.如果变量值包含空格或制表符等空白字符,会被切割分成多个参数;
而`"var"`有双引号包裹变量,仅执行变量替换,变量内容整体作为单个字符串.